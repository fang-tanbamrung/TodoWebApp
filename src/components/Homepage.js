import React from 'react';
import { Redirect } from 'react-router-dom';

const Homepage = () => {
    return(
        <div >
            {/* <Redirect to = '/todo' /> */}
        </div>
    )
}

export default Homepage;